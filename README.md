# Telegram Search Bot

Telegram-бот для поиска информации в интернете через DuckDuckGo.

## Переменная окружения

Перед запуском установите:

`BOT_TOKEN` — токен Telegram-бота.

## Render

Build Command:

`pip install -r requirements.txt`

Start Command:

`python bot.py`
