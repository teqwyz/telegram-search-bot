name: Keep Alive Render

on:
  schedule:
    # Запуск каждые 10 минут
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://telegram-search-bot-g9vr.onrender.com > /dev/null
